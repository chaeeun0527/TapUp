spring:
  application:
    name: mission_project

  datasource:
    url: jdbc:mysql://localhost:3306/mission_db?serverTimezone=Asia/Seoul&characterEncoding=UTF-8
    username: root
    password: lolpop0527@
    driver-class-name: com.mysql.cj.jdbc.Driver

  jpa:
    hibernate:
      ddl-auto: none
    show-sql: true
    properties:
      hibernate:
        format_sql: true

server:
  port: 8080